endpoints:
  enabled: false
  info:
    enabled: true

security:
  user:
    name: admin
    password: admin

management:
  security:
    role: SUPERUSER

spring:
  jpa:
    show-sql: true

info:
  build:
    name: ${name}
    version: ${version}
    git: ${revision}
